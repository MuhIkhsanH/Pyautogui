import pyautogui
import time

try:
    while True:
        x, y = pyautogui.position()
        print(f"Posisi Kursor: X = {x}, Y = {y}")
        time.sleep(0.1)  # Menunggu 100ms sebelum mendeteksi kursor lagi
except KeyboardInterrupt:
    print("\nProgram berhenti.")
